spring.datasource.url=jdbc:postgresql://localhost:5432/salario_db
spring.datasource.username=admin
spring.datasource.password=admin

# JPA / Hibernate
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=none

# JSF / JoinFaces config (defaults)
joinfaces.faces-config=/WEB-INF/faces-config.xml